# 医学考试易错题竖屏视频通用设计

## 画面

- 画布为 `1080x1920`，30 fps，一题一条。
- 支持白底与绿底两套正式主题，页面结构、字体、字号、间距和动作保持一致。
- 白底：画布 `#F4F6F8`，正文 `#15202B`，品牌蓝 `#1769E0`，内容板 `#FFFFFF`。
- 绿底：画布 `#003A46`，内容板 `#00343D`，正文 `#F7FAFA`，层级提示黄 `#F4D04F`，边框 `#6F898D`。
- 两种主题的正确答案均使用 `#18864B`，教学下划线和圈选均使用深教学红 `#B42318`，透明高亮均对应 `rgba(180,35,24,.20)`。
- 右侧预留 182 px，底部关键内容预留 300 px，避开短视频平台操作区。
- 题干和选项在讲解阶段全程保留；先放大读题，再缩小进入知识点区域。
- 字幕使用完整短句，不按固定字数切断，不使用黑底。

## 教学动作

- 读题时只圈画题眼、限定词、否定词和关键条件。
- 知识点、教研提醒、逐项辨析、答案和口诀均只标记关键词，不划整段。
- 划重点开始时间必须落在对应词语开始发音附近，结束时间不得明显晚于发音结束。
- 答案在知识点和逐项辨析完成后揭晓。
- 考点收口使用口诀，一句一行。

## 品牌与合规

- 品牌名称、课程名称、教研提醒和片尾文案全部来自 `config/project.json`。
- 中段教研提醒使用备考建议，不承诺诊断、治疗效果或考试结果。
- 页面保留低透明度品牌水印；标题位于平台安全区内。
- 绿底页面顶部 `0-200 px` 固定留空，禁止放置水印或装饰 Logo；完整的低透明度“叶片 + 易哈佛”缩小后放在右下侧空白栏，位置为 `right:24px; bottom:278px`，不得被题目板裁切。
- 片尾关注动画 4.5 秒，加号在下方，模拟点击后变为对勾。

## 禁止项

- 不写死考试科目、专业类别、账号名称或音色名称。
- 不使用数字人。
- 不朗读选项。
- 不在画面上显示制作提示、布局说明或“题干全程保留”等内部文字。
- 不覆盖旧成片和旧配置。
